# Step 1: API skeleton only.
# Step 2 adds chromadb + sentence-transformers + CPU-only torch (dense search).
# Step 3 needs nothing extra (sqlite3 is Python stdlib).
# Step 5 adds a reranker (flashrank or sentence-transformers CrossEncoder).

fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.28.0,<0.30.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
